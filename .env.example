# =============================================================================
# Palakat Monorepo Environment Variables
# =============================================================================
#
# This file contains environment variable templates for all applications in
# the monorepo. Each app should have its own .env file in its directory:
#   - apps/palakat/.env          (Mobile App)
#   - apps/palakat_admin/.env    (Admin App)
#   - apps/palakat_backend/.env  (Backend API)
#
# Copy relevant sections to each app's .env file and configure as needed.
# NEVER commit actual .env files to version control.
# =============================================================================

# =============================================================================
# PALAKAT APP (Flutter Mobile - apps/palakat/)
# =============================================================================

# Backend API endpoint
# Better to use local IP address rather than localhost or 127.0.0.1
BASE_URL=http://192.168.0.130:3000

# Google Maps integration
GOOGLE_MAPS_URL=
GOOGLE_API_KEY=

# Deep linking configuration
DEEPLINK_KEY=
DEEPLINK_HOST=
DEEPLINK_TEMPLATES=

# Error tracking
SENTRY_DSN=

# Facebook authentication
FACEBOOK_APP_ID=
FACEBOOK_PROTOCOL_SCHEME=
FACEBOOK_CLIENT_TOKEN=

# RSA encryption
RSA_PUBLIC_KEY=

# Google authentication
GOOGLE_CLIENT_ID=

# iOS app configuration
IOS_APP_ID=

# Twitter/X authentication
X_USERNAME=
X_PASSWORD=

# =============================================================================
# PALAKAT ADMIN (Flutter Admin - apps/palakat_admin/)
# =============================================================================

# Backend API endpoint for admin app
API_BASE_URL=http://192.168.0.130:3000

# Optional: API key or tokens for admin app
# API_KEY=

# =============================================================================
# PALAKAT BACKEND (NestJS - apps/palakat_backend/)
# =============================================================================

# Server port
PORT=3000

# PostgreSQL database configuration
POSTGRES_HOST=localhost
POSTGRES_DB=palakat_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_PORT=5432

# Database connection string (uses variables above)
DATABASE_POSTGRES_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"

# Force database seeding on startup (development only)
FORCE_SEEDING=false

# Application client credentials for frontend authentication
APP_CLIENT_USERNAME="your-frontend-username"
APP_CLIENT_PASSWORD="your-frontend-password"

# JWT secret for token signing (use a strong random string in production)
JWT_SECRET="your-very-long-and-secure-jwt-secret-key"

# =============================================================================
# FIREBASE CONFIGURATION (Shared - for Flutter apps)
# =============================================================================
#
# Firebase configuration is managed through configuration files:
#   - apps/palakat/android/app/google-services.json
#   - apps/palakat/ios/Runner/GoogleService-Info.plist
#   - apps/palakat_admin/web/firebase-config.js (if using web)
#
# Download these files from your Firebase console and place them in the
# appropriate directories. Do NOT commit these files to version control.
# =============================================================================
